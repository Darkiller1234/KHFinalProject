# 이미지 설정 (OpenJDK 24)
FROM openjdk:24-jdk-slim

# 컨테이너 내에 작업할 경로
WORKDIR /app

# 실행할 자바 파일 복사
# (도커 서버로 전달받은 코드를 복사해 실행할 예정이므로, 여기에 미리 추가할 필요는 없음)
COPY . /app

# 실행 명령 설정 (기본적으로 실행될 명령어 설정)
# (실제 자바 파일 실행 명령어는 컨테이너 실행 시 동적으로 설정)
CMD ["java", "-version"]